export class LoginOptions {
    currentClass?: string;
    teacher?: string[]; // posibly parent and teacher
    roles?: string[]; // need to bring in roles from user in core module
}
// need to see how teacher will be stored maybe by student or by teacher
